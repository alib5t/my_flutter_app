workflows:
  ios_ipa:
    name: iOS IPA (Working)

    environment:
      flutter: stable
      xcode: latest

    scripts:
      - cd my_app
      - flutter clean
      - flutter pub get
      - flutter build ipa --no-codesign

    artifacts:
      - my_app/build/ios/ipa/**/*.ipa
