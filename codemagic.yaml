workflows:
  ios-app-build:
    name: iOS Build
    environment:
      flutter: stable

    scripts:
      - cd my_app
      - flutter pub get
      - flutter build ios --release --no-codesign

    artifacts:
      - build/ios/iphoneos/Runner.app
